Pancake Live Sales Monitor v1.4.0

FINAL PIPELINE FIX

1) Employee Statistic empty shop fixed
   success:true + data:[] + summary:{} = 0 baht
   This is not an error and does not HOLD the whole snapshot.

2) Still fail closed
   Permission errors, timeouts, malformed responses and ambiguous summaries are NOT converted to zero.

3) Complete snapshot only
   No partial total is ever published.
   +/- only occurs between complete verified snapshots.

4) Cross-device stable cutoff
   All devices align live queries to the same 10-second reporting cutoff with a 2-second safety lag.

5) Timeout-safe batching
   6 shops per serverless batch, max 3 batch requests in parallel, server retry + client batch retry.

6) Accurate incomplete count
   One failed shop no longer gets reported as if all 52 shops were incomplete.

7) Tested with 52 unique shops / 3 API accounts / 9 batches including a zero-sales shop.
