Pancake Live Sales Monitor v1.2.5

FIXED
- Empty/blank dashboard when /api/sales fails or times out.
- Live endpoint no longer waits for 5-day history.
- Added /api/history for 5-day cards (refresh every 5 minutes).
- Better Pancake API error message when HTTP 200 contains success=false.
- History fallback requests are parallel instead of sequential.

DEPLOY
Replace these files in the existing repo and commit to main:
- public/app.js
- lib/core.mjs
- lib/handlers.mjs
- api/history.js (new)

Recommended: deploy the full project zip if easier.
