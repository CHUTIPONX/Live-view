Pancake Live Sales Monitor v1.6.1

SECURITY UPDATE
- Added real server-side CSRF protection for authenticated POST APIs.
- Added same-origin mutation validation.
- Added login throttle (10 attempts / 10 minutes per observed IP, best effort).
- Added strict CSP and security headers.
- Added /api/security-token for existing sessions and automatic CSRF refresh.
- Added client security guard on dashboard/login/settings.
- Added red Inspect/source warning:
  โค้ดกูอย่ายุ่งไอหน้าปลาดุกน๊อคน้ำ
- Blocks common DevTools/source shortcuts and context menu outside form inputs.
- Existing sales, order animation and 100-video scenic behavior are unchanged.

IMPORTANT
No website can prevent the owner of a browser/device from inspecting code that must be delivered to that browser.
This build protects secrets and server writes even if Inspect is opened; the browser warning is only an extra deterrence layer.
