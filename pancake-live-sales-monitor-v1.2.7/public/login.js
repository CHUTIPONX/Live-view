const form=document.querySelector('#loginForm'), err=document.querySelector('#loginError'), usernameEl=document.querySelector('#username'), passwordEl=document.querySelector('#password');
form.addEventListener('submit',async e=>{e.preventDefault();err.classList.add('hidden');const btn=form.querySelector('button');btn.disabled=true;btn.querySelector('span').textContent='Signing In...';try{const r=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:usernameEl.value,password:passwordEl.value})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Sign in failed');location.href='/';}catch(e){err.textContent=e.message;err.classList.remove('hidden');}finally{btn.disabled=false;btn.querySelector('span').textContent='Sign In';}});
