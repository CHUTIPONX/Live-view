Pancake Live Sales Monitor v1.2.4 HOTFIX

WHY THE LIVE SITE SHOWED 0 / ERROR
- The deployed GitHub repo was still using the older grouped-by-date parser.
- Pancake returned HTTP 200, but the code did not read the live response summary correctly.

FIX
- Today total reads Pancake statistics summary.price directly.
- Converts satang to baht (/100).
- Orders read summary.order_count.
- Supports summary at root, data.summary, result.summary, or success.summary.
- If Pancake changes the response again, the error now includes safe response-shape keys for debugging.
- 1-second polling, +/- animation, no database, and last-good retention remain unchanged.

FAST APPLY
1. In GitHub repo CHUTIPONX/Live-view open lib/core.mjs.
2. Replace it with lib/core.mjs from this package.
3. Commit directly to main.
4. Wait for Vercel production deployment to finish, then hard refresh the live page.
