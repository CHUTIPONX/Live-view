{
  "name": "pancake-live-sales-monitor",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "description": "Pancake POS live sales monitor with fixed-cutoff client-orchestrated report batching and complete-snapshot-only totals",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  }
}
