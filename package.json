{
  "name": "pancake-live-sales-monitor",
  "version": "1.9.0",
  "private": true,
  "type": "module",
  "description": "Pancake POS live sales monitor with 100 Worlds smart-shuffle procedural atmospheres, boosted compressed sale audio, verified live orders, Facebook Page discovery, multi-account POS Shop auditing, and sales-metric diagnostics",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  }
}
