{
  "name": "pancake-live-sales-monitor",
  "version": "1.7.8",
  "private": true,
  "type": "module",
  "description": "Pancake POS live sales monitor with hit-synchronized Latest 5 feed, page/product/code/price metadata, resilient order lookup, verified score hits, page checks, sale chimes, security, and seasonal atmospheres",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  }
}
