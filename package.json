{
  "name": "pancake-live-sales-monitor",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "description": "Pancake POS live sales monitor with verified per-order score animation and 100-video full-length scenic playlist",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  }
}
