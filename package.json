{
  "name": "pancake-live-sales-monitor",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "Pancake POS Employee Statistic monitor with authoritative zero-sales handling, stable cross-device cutoffs, timeout-safe batching, and complete-snapshot-only totals",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  }
}
