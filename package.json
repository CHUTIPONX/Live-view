{
  "name": "pancake-live-sales-monitor",
  "version": "1.5.1",
  "private": true,
  "type": "module",
  "description": "Pancake Employee Statistic live sales monitor with exact per-order delta queue, unlimited accounts, complete snapshots, and seasonal video backgrounds",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts-check.mjs",
    "selftest": "node scripts-selftest.mjs",
    "test": "npm run check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  }
}
